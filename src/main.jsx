import React from "react";
import ReactDOM from "react-dom/client";
import Compidant from "./Compidant.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Compidant />
  </React.StrictMode>,
);
